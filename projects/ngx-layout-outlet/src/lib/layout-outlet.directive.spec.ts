import { LayoutOutletDirective } from './layout-outlet.directive';

describe('LayoutOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutOutletDirective();
    expect(directive).toBeTruthy();
  });
});
